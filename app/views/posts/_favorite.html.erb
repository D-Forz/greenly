<%= turbo_frame_tag "Favorite-#{post.id}" do %>
  <% if post.favorites.find_by(favoritable_id: post.id, user: current_user) %>
    <%= link_to '💔', favorite_path(post.id), data: {turbo_method: :delete} %>
  <% else %>
    <%= link_to '💖', favorites_path(post_id: post.id), data: {turbo_method: :post} %>
  <% end %>
<% end %>
